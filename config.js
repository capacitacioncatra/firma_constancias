// Configuración de Google Sheets
// IMPORTANTE: Reemplaza esta URL con la URL de tu Google Apps Script
const CONFIG = {
    // URL del Google Apps Script (la obtendrás después de implementar el script)
    SHEETS_API_URL: 'TU_URL_AQUI',
    
    // Puedes cambiar esto a false para volver a localStorage en desarrollo
    USE_GOOGLE_SHEETS: true
};
